{
  "name": "@jsonos/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --config vite.content.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@jsonos/redact-core": "file:../../packages/redact-core"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^24.12.2",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
